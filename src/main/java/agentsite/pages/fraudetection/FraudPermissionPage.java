package agentsite.pages.fraudetection;

import agentsite.pages.HomePage;

public class FraudPermissionPage extends HomePage {
    public FraudPermissionPage(String types) {
        super(types);
    }
}
